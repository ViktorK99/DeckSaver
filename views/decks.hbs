<div class="container mt-3">
    <div class="row">
        {{#each discordDecks}}
            <div class="list-group col-12 col-md-4 mb-3">
                <a href="#" class="list-group-item list-group-item-action active disabled">{{@key}}</a>
                {{#each this}}
                <a href="#" class="list-group-item list-group-item-action">{{this.deckName}}</a>
                {{/each}}
            </div>
        {{/each}}
    </div>
</div>